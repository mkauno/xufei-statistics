---
name: 统计续费
description: >
  自动汇总语文、数学、英语三科续费数据，生成"幼小拼音汇总"和"总体合计汇总"两张报表。
  支持小高综合续班数据表合并（素养E→英语，素养M→数学）。
  小高综合的续费数据按指定的续费日期列处理（如5.10续班）。
  总体合计中语文教师数据不含拼音学科。
  当用户说"统计续费"、"汇总续费"、"续费统计"、"做续费表"时触发本 Skill。
agent_created: true
---

# 统计续费 Skill

## 用途

当用户提及"统计续费 / 汇总续费 / 续费统计 / 做续费表"时，按本流程自动汇总语文、数学、英语三科的基数和续费数据，输出标准化报表。

## 数据来源与关键字段

- **数据来源**：语文、数学、英语三个原始数据表（Excel），可选小高综合续班数据表
- **关键字段**：上课校区、课程年级、任课老师、基数、是否续费3季度
- **小高综合续班数据表**：使用"续班数据表"sheet，学科(素养E/素养M)、任课老师、基数、5.10续班
  - 素养E → 合并到英语
  - 素养M → 合并到数学
- **续费日期列规则**：综合数据的续费列根据用户指定的日期列处理（如5.10续班、5.14续班等）。
  - 有综合文件时：按指定日期列汇总
  - 仅单文件时：按文件自身的续费字段（是否续费3季度）处理
- **文件数量规则**：仅1个源文件时处理单文件数据；提供小高综合文件时自动合并处理
- **注意**：.xls后缀的文件可能实际为xlsx格式，用openpyxl引擎读取；原始xlsx需用calamine引擎（openpyxl不兼容）

## 统计口径

- 基数 = SUM(基数列)
- 续费 = SUM(是否续费3季度列) 或 SUM(5.10续班列)
- 空值按 0 处理；保留 0.5 等半数续费
- 续费率 = 续费 / 基数

## 执行步骤

1. 读取语文 / 数学 / 英语表头，确认字段位置（上课校区、课程年级、任课老师、基数、是否续费3季度）。
2. 生成**「幼小拼音汇总」**：筛选 语文-幼小、语文-拼音、数学-幼小、英语-幼小 四组数据。
3. 在「幼小拼音汇总」中依次输出：
   - 总体汇总（四组合并）
   - 各分组教师汇总（按语文-幼小 / 语文-拼音 / 数学-幼小 / 英语-幼小 分别列出教师）
   - 校区汇总
4. 生成**「总体合计汇总」**：使用语文(排除拼音) / 数学 / 英语三科数据。
5. 如果提供小高综合续班数据表（续班数据表 sheet），将素养M合并入数学、素养E合并入英语：
   - 数学(含素养M): 基数=原数学基数 + SUM(素养M基数), 续费=原数学续费 + SUM(素养M.5.10续班)
   - 英语(含素养E): 基数=原英语基数 + SUM(素养E基数), 续费=原英语续费 + SUM(素养E.5.10续班)
   - 教师汇总：concat后groupby('任课老师').agg(sum)合并同名教师
   - 校区汇总：concat后groupby('上课校区').agg(sum)合并同校区
6. 在「总体合计汇总」中依次输出：
   - 总体数据（三科合并，含小高综合标注）
   - 校区汇总（语文 / 数学含素养M / 英语含素养E）
   - 语文教师汇总
   - 数学教师汇总（含素养M）
   - 英语教师汇总（含素养E）
7. 所有汇总区块按**续费率从高到低**排列；续费率相同时再按基数降序。
8. 续费列格式：整数不显示小数点；含 0.5 等非整数保留 1 位小数。
9. 设置标题、表头、数字格式、列宽，并检查空表头和错误值。

## 输出表格

| 表名 | 包含数据 | 排序规则 |
|------|----------|----------|
| 幼小拼音汇总 | 语文-幼小 / 语文-拼音 / 数学-幼小 / 英语-幼小 | 含续费率，按续费率降序 |
| 总体合计汇总 | 语文 / 数学(含素养M) / 英语(含素养E)全量总体、校区、分学科教师 | 含续费率，按续费率降序 |

## 格式规则

| 列 | 格式代码 | 说明 |
|----|----------|------|
| 基数列 | `#,##0` | 整数显示，千位分隔符 |
| 续费列 | 整数：`#,##0`；非整数：`#,##0.0` | 避免出现"222."这类尾点 |
| 续费率列 | `0.0%` | 所有汇总表均需要 |

## 校验清单（完成后逐项核对）

1. 总体合计 = 语文基数+续费 + 数学基数+续费 + 英语基数+续费
2. 各校区汇总按三科全量合并，不重复不遗漏
3. 教师汇总必须分语文、数学、英语分别输出，不混合
4. 续费列整数无小数，0.5 保留一位
5. 无空表头、无 `#REF! / #DIV/0! / #VALUE!` 等错误
6. 小高综合合并时：校验原基数+素养基数=合并后基数，原续费+素养续费=合并后续费

## 幼小拼音续班率奖金计算

每次统计完成后，在「幼小拼音汇总」sheet 末尾自动生成两张表：

### 续班完成率教师名单

按阈值统计英语-幼小、数学-幼小两组教师（语文-幼小/拼音不参与）：

- **已完成100%教师**：续费率=100%的教师名单
- **已完成90%教师**：90%≤续费率<100%的教师名单
- **已完成80%教师**：80%≤续费率<90%的教师名单

### 续班率奖金表

**适用科目**：英语-幼小、数学-幼小（目标≥80%）
**奖金规则**：
- 达成续费 = ceil(基数 × 80%)
- 超出续费 = 实际续费 - 达成续费
- 奖金 = 达成续费 × 200元 + 超出续费 × 500元

输出格式：姓名、学科、奖金金额 三列，按奖金金额降序排列。

## 默认交付说明

完成后回复：**「已更新"幼小拼音汇总"和"总体合计汇总"，总基数 X，总续费 Y，续费率 Z%，校验通过。」**

## 详细参考

见 `references/续费skill原文.md`，其中包含 skill 原始规则全文。
